@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var stuff = ViewData["stuff"];
    List<String> logins = (List<String>)ViewData["logins"];
    int cnt = 0;
}

<ul>
    @foreach (var cur in (List<List<StuffMember>>)ViewData["stuff"])
    {
        <li>
        @foreach (var cur2 in (List<StuffMember>)cur)
        {
            @cur2.Login;
        }
        </li>
    }
</ul>